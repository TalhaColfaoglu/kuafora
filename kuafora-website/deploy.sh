#!/bin/bash
# Deprecated: centralized deployment moved to repo root
set -e
echo "This project now uses a centralized deployment at the repo root."
echo "Use: docker compose up -d --build (from repo root)"
